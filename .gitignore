# macOS
.DS_Store

# Сурови източници (слой raw/) — защитени с авторско право, пазят се локално
raw/*.pdf
raw/**/*.pdf
raw/*.epub
raw/**/*.epub
raw/assets/

# Експорти/генерирани файлове (docx/pdf/изображения, build-скриптове) — пазят се локално
exports/
